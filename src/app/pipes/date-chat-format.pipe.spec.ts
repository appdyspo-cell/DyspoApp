import { DateChatFormatPipe } from './date-chat-format.pipe';

describe('DateChatFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DateChatFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
